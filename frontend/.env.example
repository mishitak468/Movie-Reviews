# default: use the in-app mock (no backend needed)
VITE_USE_MOCK=true

# when the backend is live, set VITE_USE_MOCK=false.
# leave VITE_API_URL unset to use the "/api" proxy (recommended — single origin),
# or point it at a full url for cross-origin dev.
# VITE_API_URL=http://localhost:5000/api
